# Copy to inventory.ini and replace IPs with Terraform outputs.

[master]
master-1 ansible_host=<MASTER_PUBLIC_IP> ansible_user=ubuntu

[worker]
worker-1 ansible_host=<WORKER_PUBLIC_IP> ansible_user=ubuntu

[k3s_cluster:children]
master
worker
